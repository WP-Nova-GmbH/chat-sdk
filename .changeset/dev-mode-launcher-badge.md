---
"@wp-nova/sdk": minor
---

Badge the launcher for development-mode surfaces. When the embedded session is minted for a surface in development origin mode, the token grant now carries a `developmentMode` flag and the launcher renders an amber warning ring and a "DEV" pill — making test embeds unmistakable without altering the surface's own accent color or icon.
